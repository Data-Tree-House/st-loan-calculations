[project]
name = "loan-calcs"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.12"
dependencies = ["streamlit", "loguru", "plotly"]

[dependency-groups]
dev = ["pytest", "pytest-cov"]
